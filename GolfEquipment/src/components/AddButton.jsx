const AddButton = () => {
    const handleClick = () => {
        console.log('Add Club Form');
        
    }

    return (
        <button onClick={handleClick}>Add a Club</button>
    )
}

export default AddButton